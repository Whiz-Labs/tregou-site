# Supabase — used by the landing quiz (/comecar) to store anonymous responses.
# The anon key is PUBLIC and safe to expose: RLS on quiz_responses allows
# insert-only for the anon role (no read/update/delete). Use the PROD (fpox) project.
# If these are unset, the quiz still works and simply skips saving.
PUBLIC_SUPABASE_URL=https://fpoxalunsjzatvkghouw.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-fpox-anon-key-here
